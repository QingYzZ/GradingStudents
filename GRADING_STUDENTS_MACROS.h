/* this file is used to define some Macros that is used 
 * across the files in doc_manager
 */

// Constants for maximum sizes
const int MAX_NUM_STUDENTS = 100;
const int MIN_SUBMISSIONS = 2;	/* a student need to at least try */
const int MIN_TESTS = 1; /* at least student tests should be included */
const int DEFAULT_MAX_SUBMISSIONS = 40; /* default number of max submissions */

// Constants for success and failure
const int SUCCESS = 0;
const int FAILURE = -1;